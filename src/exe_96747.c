extern int __near mn63E2;

int far exe_96747(void)
{
    _asm {
        mov ax,mn63E2
        sub dx,dx
        add ax,ax
        adc dx,dx
        add ax,ax
        adc dx,dx
        add ax,ax
        adc dx,dx
        add ax,ax
        adc dx,dx
        retf
    }
}
